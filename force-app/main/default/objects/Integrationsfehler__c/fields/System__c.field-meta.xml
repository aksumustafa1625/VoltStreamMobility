<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>System__c</fullName>
    <description>Which external system the exchange was with, e.g. OCPI. Kept as text rather than a picklist so adding a system does not require a metadata change during an incident.</description>
    <externalId>false</externalId>
    <label>System</label>
    <required>false</required>
    <length>40</length>
    <unique>false</unique>
    <type>Text</type>
</CustomField>
