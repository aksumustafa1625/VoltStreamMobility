<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Kennung__c</fullName>
    <description>The EVSE-ID or location id the failure belongs to, when the failure is about one record rather than the whole call. A batch that fails on one of eight hundred rows must say which.</description>
    <externalId>false</externalId>
    <label>Betroffene Kennung</label>
    <required>false</required>
    <length>80</length>
    <unique>false</unique>
    <type>Text</type>
</CustomField>
