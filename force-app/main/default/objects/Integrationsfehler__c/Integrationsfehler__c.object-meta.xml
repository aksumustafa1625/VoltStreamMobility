<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>One row per failed exchange with an external system. An import that swallows its errors reports an empty fleet as a clean fleet, which is the same failure the compliance engine refuses to make: silence is not compliance. The payload is kept so a failure can be diagnosed without asking the CPO to reproduce it.</description>
    <enableActivities>false</enableActivities>
    <enableHistory>false</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <label>Integrationsfehler</label>
    <nameField>
        <displayFormat>IF-{00000}</displayFormat>
        <label>Nummer</label>
        <type>AutoNumber</type>
    </nameField>
    <pluralLabel>Integrationsfehler</pluralLabel>
    <sharingModel>ReadWrite</sharingModel>
</CustomObject>
